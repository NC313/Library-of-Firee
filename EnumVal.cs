namespace Library_of_Fire
{
    public enum EnumVal
    {
        DisplayBooks = 1,
        SearchByAuthor = 2,
        SearchByTitle = 3,
        CheckOut = 4,
        ReturnBook = 5,
        Exit = 6
    }
}
